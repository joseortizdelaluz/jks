import { DecimalX100Pipe } from './decimal-x100.pipe';

describe('DecimalX100Pipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalX100Pipe();
    expect(pipe).toBeTruthy();
  });
});
